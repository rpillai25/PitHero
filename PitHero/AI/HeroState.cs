namespace PitHero.AI
{
    /// <summary>
    /// Actor state for the simplified state machine
    /// </summary>
    public enum ActorState
    {
        Idle,
        GoTo,
        PerformAction
    }
}